<!--<p>Welcome <%#= @email %>!</p>-->

<!--<p>You can confirm your account email through the link below:</p>-->

<!--<p><%#= link_to 'Confirm my account', confirmation_url(@resource, confirmation_token: @token) %></p>-->

<div class="container mt-5">
  <div class="card p-4">
    <h3 class="card-title text-center mb-4">Welcome <%= @email %>!</h3>
    <p class="card-text">You can confirm your account email through the link below:</p>
    <p class="text-center">
      <%= link_to 'Confirm my account', confirmation_url(@resource, confirmation_token: @token), class: 'btn btn-primary' %>
    </p>
  </div>
</div>